syntax = "proto3";
option go_package = "awesomeProject3/grpc/proto";

message Students {
  repeated string students = 1;
}

message empty {

}

service ITUdatabase {
  rpc GetStudents (empty) returns (Students) {};
}